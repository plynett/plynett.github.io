# External Scripts

These files are local third-party or worker-style helper scripts. They are not part of the core coastal-wave solver, but they support optional export/readback paths.

Do not refactor these files as if they were first-party model code. Treat them as vendored dependencies unless there is a specific bug or replacement plan.

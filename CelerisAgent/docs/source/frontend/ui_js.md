# `js/ui.js`

Shared frontend DOM helpers.

Current exports:

- `setEmpty(element, className, text)`: standard empty-panel rendering.

Keep this module intentionally small. Add helpers only when at least two modules need the same DOM behavior.

# `js/format.js`

Small formatting and escaping helpers used by render modules.

Current exports:

- `formatBytes(value)`
- `formatCoord(value)`
- `formatNumber(value)`
- `escapeHtml(value)`

Keep helpers deterministic and side-effect free.
